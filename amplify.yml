version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm run build
    build:
      commands:
        - echo "Build completed"
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
